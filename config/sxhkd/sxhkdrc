# BSPWM Bindings

# Quit BSPWM
super + shift + Escape
    bspc quit

# Restart BSPWM
super + shift + w
    bspc wm -r

# Kill window
super + shift + q
    crystal --close

# Focus on window direction
super + {h, j, k, l}
    bspc node -f {west, south, north, east}

# Send window to desktop
super + shift + {0-9}
    crystal --send {0-9}

# Send window to desktop and swap to desktop
super + control + {0-9}
    bspc node -d {0-9} && bspunhide && bspc desktop -f {0-9}

# Swap nodes the three from the current nodes parent
super + control + {Up, Down}
    bspc node @parent -C {forward, backward}

# Rotate the three from the current nodes parent
super + control + {Right, Left}
    bspc node @parent --rotate {90, 270}

# Flip the three from the current nodes parent
super + control + {h, j, k, l}
    bspc node @parent -F {vertical, horizontal, horizontal, vertical}

# Toggle fullscreen on and off
super + f
    crystal --fullscreen

# Toggle floating on and off
super + o
    bspc node focused.tiled -t floating || bspc node focused.floating -t tiled

# Swap window with direction
super + shift + {h, j, k, l}
    bspc node -s {west, south, north, east}

# Focus on desktop
super + {0-9}
    crystal --focus {0-9}

# Cycle through desktops
super + alt + {Left, Right}
    crystal --focus {prev, next}

# Expand a window by moving one of its side outward
ctrl + alt + shift + {h, j, k, l}
    bspc node -z {right -20 0, top 0 20, bottom 0 -20, left 20 0}

# Launc Rofi
super + d
    rofi -show
